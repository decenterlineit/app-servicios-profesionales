export function searchProfessionals(items,filters={}){let out=items.filter(p=>p.status==='active');const q=(filters.query||'').toLowerCase();if(q)out=out.filter(p=>[p.name,...p.specialties].join(' ').toLowerCase().includes(q));if(filters.category)out=out.filter(p=>p.categoryIds.includes(filters.category));if(filters.rating)out=out.filter(p=>p.rating>=Number(filters.rating));if(filters.verified)out=out.filter(p=>p.verified);if(filters.today)out=out.filter(p=>p.availableToday);if(filters.distance)out=out.filter(p=>p.distanceKm<=Number(filters.distance));return [...out].sort((a,b)=>filters.sort==='rating'?b.rating-a.rating:filters.sort==='distance'?a.distanceKm-b.distanceKm:filters.sort==='price'?a.completedJobs-b.completedJobs:(b.rating*b.reviewsCount)-(a.rating*a.reviewsCount));}
