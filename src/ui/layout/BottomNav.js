export function BottomNav(path){const items=[['/','fa-solid fa-house','Inicio'],['/search','fa-solid fa-magnifying-glass','Buscar'],['/bookings','fa-solid fa-calendar-check','Reservas'],['/messages','fa-solid fa-comment-dots','Mensajes'],['/account','fa-solid fa-user','Cuenta']];return `<nav class="bottom-nav">${items.map(([href,i,label])=>`<a class="${path===href||href!=='/'&&path.startsWith(href)?'active':''}" href="#${href}"><i class="${i}" aria-hidden="true"></i><small>${label}</small></a>`).join('')}</nav>`}
