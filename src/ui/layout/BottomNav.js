export function BottomNav(path){const items=[['/','⌂','Inicio'],['/search','⌕','Buscar'],['/bookings','▣','Reservas'],['/messages','✉','Mensajes'],['/account','●','Cuenta']];return `<nav class="bottom-nav">${items.map(([href,i,label])=>`<a class="${path===href||href!=='/'&&path.startsWith(href)?'active':''}" href="#${href}"><span>${i}</span><small>${label}</small></a>`).join('')}</nav>`}
